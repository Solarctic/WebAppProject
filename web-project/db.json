{
  "users": [
    { "id": 1, "name": "admin", "password": "admin123" },
    { "id": 2, "name": "user", "password": "user123" }
  ]
}

// this a fake db for backend used for testing purposes

// json-server --watch db.json --port 3000
// npm run json-server


